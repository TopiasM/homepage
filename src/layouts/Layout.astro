<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta
      name="description"
      content="I enjoy developing performant and well-designed web experiences"
    />
    <title>Topias Martikainen - Software Engineer</title>
  </head>
  <body class="bg-zinc-950">
    <div id="grain"></div>
    <slot />
  </body>
</html>

<style>
  body {
    margin: 0;
    min-width: 100vw;
    min-height: 100vh;
  }

  #grain {
    position: fixed;
    width: 100vw;
    height: 100vh;
    overflow: hidden;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='2.29' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
    opacity: 0.11;
    -webkit-animation: noise 0.5s steps(2) infinite;
    animation: noise 0.5s steps(2) infinite;
    pointer-events: none;
    z-index: 9;
  }

  @keyframes noise {
    0% {
      transfrom: rotate(0turn);
    }

    100% {
      transform: rotate(1turn);
    }
  }
</style>
